<UserControl
    x:Class="Boards_WP.Views.NotificationsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:Boards_WP.ViewModels"
    xmlns:local="using:Boards_WP.Views">

    <Border Background="{x:Bind ViewModel.MainViewModel.VividThemeBrush, Mode=OneWay}" CornerRadius="15" Margin="4">
        <Grid Padding="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Notifications" 
                       FontSize="18" 
                       FontWeight="SemiBold" 
                       Foreground="#FFFFFF" 
                       Margin="0,0,0,15"/>

            <ListView x:Name="NotificationsList" 
                  Grid.Row="1"
                  ItemsSource="{x:Bind ViewModel.Notifications, Mode=OneWay}" 
                  SelectionMode="Single"
                  IsItemClickEnabled="True"
                  ItemClick="NotificationsList_ItemClick"
                  Padding="0">

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,10"/>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:NotificationItemViewModel">
                        <Grid x:Name="RootGrid">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    
                                    <VisualState x:Name="Normal" />

                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="NotificationCard.BorderBrush" Value="#6A67E1"/>
                                            <Setter Target="NotificationCard.BorderThickness" Value="2"/>
                                            <Setter Target="NotificationCard.Background" Value="#F8F8FF"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <Border x:Name="NotificationCard"
                                    Background="White" 
                                    BorderBrush="#E0E0FF" 
                                    BorderThickness="1" 
                                    CornerRadius="10" 
                                    Padding="12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{x:Bind Message, Mode=OneWay}" 
                                               FontSize="13" 
                                               FontWeight="{x:Bind MessageFontWeight, Mode=OneWay}"
                                               Foreground="#333333" 
                                               TextWrapping="Wrap"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="1" 
                                               Text="{x:Bind Time, Mode=OneWay}" 
                                               FontSize="11" 
                                               Foreground="Gray" 
                                               Margin="10,0,0,0"
                                               VerticalAlignment="Bottom"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.Footer>
                    <Button Content="Load More" 
                    Command="{x:Bind ViewModel.LoadBatchCommand}"
                    Visibility="{x:Bind GetVisibility(ViewModel.HasMore), Mode=OneWay}"
                    HorizontalAlignment="Center" 
                    Margin="10"
                    Background="#6A67E1" 
                    Foreground="White" 
                    CornerRadius="8"/>
                </ListView.Footer>
            </ListView>
        </Grid>
    </Border>
</UserControl>